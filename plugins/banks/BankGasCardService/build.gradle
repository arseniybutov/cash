jar.archiveName = "bank-gascardservice.jar"

packaging {    
    type = 'plugin'
    deployPath = 'modules/bank/plugins'
}

dependencies {
    compile project(':SetRetail10_Commons:DataStructsModule'), 
            project(':SetRetail10_Cash:modules:BankModule'),
            libs.logging
    testCompile libs.junit,
                libs.fest
}

