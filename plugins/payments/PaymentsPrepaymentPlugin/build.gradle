jar.archiveName = 'payments-prepayment.jar'

packaging {
    branch = 'POS, BELARUS, LENTA';
}

dependencies {
    compile project(':SetRetail10_Cash:modules:VisualInterface'),
		project(':SetRetail10_Commons:DataStructsModule'), 
		project(':SetRetail10_Cash:modules:Payments'),
		project(':SetRetail10_Commons:PaymentsPrepaymentEntity'),
		project(':SetRetail10_Commons:SET-Cards-PresentCards'),
            libs.logging
    testCompile libs.junit
}
