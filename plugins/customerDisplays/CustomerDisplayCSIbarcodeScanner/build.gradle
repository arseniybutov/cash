jar.archiveName = 'customerDisplay-csibarcodescanner.jar'

dependencies {
    compile project(':SetRetail10_Cash:modules:CustomerDisplayModule'),
            project(':SetRetail10_Cash:modules:BarcodeScannerModule'),
            libs.logging
    testCompile libs.junit
}
