jar.archiveName = 'customerDisplay-viafiscalprinter.jar'

packaging {
    type = 'library'
	deployPath = 'lib';
}

dependencies {
    compile project(':SetRetail10_Cash:modules:CustomerDisplayModule'), 
            project(':SetRetail10_Cash:modules:FiscalPrinterModule'),
            libs.logging
    testCompile libs.junit
}
