jar.archiveName = 'externalMSR-poslab.jar'

packaging {
    type = 'plugin'
    deployPath = 'modules/externalMSR/plugins'
}

dependencies {
    compile project(':SetRetail10_Commons:DataStructsModule'), project(':SetRetail10_Cash:modules:ExternalMSRModule')
    testCompile project(':SetRetail10_Cash:modules:KeyboardModule'), libs.junit
}
